/**
 * Transforms a color string like `rgb(0,0,0)` to a color string in hex syntax like `#000000`
 */
export declare function rgb2Hex(s: string): string;
export declare function randomCssColor(): string;
